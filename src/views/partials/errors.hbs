{{#if error}}
<div class="notification is-danger">
    <button class="delete"></button>
        <p>{{error}}.</p>
</div>
{{/if}}

{{#if errors}}
<div class="notification is-danger">
    <button class="delete"></button>
    {{#each errors}}
        <p>{{text}}.</p>
    {{/each}}
</div>
{{/if}}